config {
  type: "view",
  schema: "dataform_staging",
  tags: ["staging"]
}

SELECT
  user_id,
  name,
  -- If value is null then set 'Standard' value.
  COALESCE(membership_tier, 'Standard') as membership_tier,
  signup_date
FROM
  ${ref("users")}